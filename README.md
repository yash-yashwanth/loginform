# loginform

A simple React login form with validation and inline error messages.

## Setup

1. Install dependencies:
   ```bash
   npm install
   ```
2. Start the development server:
   ```bash
   npm run dev
   ```

## Features

- Email required validation
- Password required validation
- Password minimum length validation (6 characters)
- Simple React `useState` form state handling
